{
  "private": true,
  "scripts": {
    "sass": "node-sass -o assets scss --output-style compressed",
    "watch": "onchange 'scss/**/*.scss' -- yarn run sass"
  },
  "devDependencies": {
    "node-sass": "^4.5.3",
    "onchange": "^3.2.1"
  }
}
